<html lang="en">
<head>
      <meta charset="UTF-8">
      <meta name="viewport" content="width=device-width, initial-scale=1.0">
      <title>Node js</title>
      <script src="https://cdn.tailwindcss.com"></script>
      <link rel="stylesheet" href="stylesheets/style.css">
</head>
<body>
      <div class="w-full h-full bg-zinc-900 p-10">
            <div class="nav mb-10 ">
                  <a class="text-3xl text-blue-500" href="/read">read users</a>
                  
            </div>
            <h1 class=" text-white mb-5 text-3xl tracking-tighter">Update user</h1>
            <form action="/update/<%= user._id %>" method="post">
                  <input  class="px-5 py-2 border-2 border-zinc-80 bg-transparent rounded-md text-white outline-none" 
                   type="text" 
                   placeholder="name"
                   name="name"
                  value="<%= user.name %>"
                    id="">
                    <input  class="px-5 py-2 border-2 border-zinc-80 bg-transparent rounded-md text-white outline-none" 
                   type="text" 
                   placeholder="email Update"
                   name="email"
                   value="<%= user.email %>"

                    id="">
                    <input  class="px-5 py-2 border-2 border-zinc-80 bg-transparent rounded-md text-white outline-none" 
                   type="text" 
                   placeholder="image Update"
                   name="image"
                   value="<%= user.image %>"

                    id="">
                    <input  class="px-5 py-2 border-2 border-zinc-80 bg-yellow-500 rounded-lg text-white outline-none ml-3" 
                   type="submit" 
                   
                   name="Update user"
                   value="Update user">
                  
            </form>
      </div>
</body>
</html>